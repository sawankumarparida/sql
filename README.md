# Business Analytics SQL Sandbox (`ba_sandbox`)

An enterprise-grade relational database sandbox built natively in Linux (WSL MySQL) designed to model corporate transaction systems, execute advanced analytical queries, and demonstrate database lifecycle management (Backup & Disaster Recovery).

## 🚀 Project Overview
This repository contains the database schema, sample datasets, and core analytical scripts used to solve real-world corporate problems. It transitions standard relational data structures into actionable executive insights using advanced SQL mechanisms.

---

## 📊 Database Architecture (Schema)
The repository models an E-commerce or B2B Enterprise distribution system consisting of four highly normalized relational tables:

```text
your-project-repository/
└── ba_sandbox_backup.sql  <- Full database dump containing schema + data
